@model Lab3A.Models.Doctor

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

@Html.ActionLink("Додади пациент", "AddPatient", new { id = Model.Id })

<div>
    <h4>Doctor</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Hospital.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Hospital.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>

// ------------------------------> ova so <ul> e dodaeno

<ul>
    @foreach (var p in Model.Patients)
    {
        <li>@Html.ActionLink(p.Name, "Details", "Patient", new { id = p.Id }, null)</li>
    }
</ul>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
